name: Run Cloud Agent
on: [push]

jobs:
  test-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Verify Agent
        run: echo "The cloud agent is officially enabled and running!"
